all:
  vars:
    ansible_user: ubuntu
    ansible_ssh_private_key_file: ~/.ssh/misskey_blue.key
  children:
    servers:
      hosts:
        misskey-blue:
          ansible_host: 203.0.113.10
        media-proxy:
          ansible_host: 203.0.113.20
  # migrate.yml実行時のみ定義するエイリアス(compose_projectは両方に必要)
  # hosts:
  #   new:
  #     ansible_host: 203.0.113.10
  #     migration_host: 203.0.113.10
  #     compose_project: misskeyblue-docker-provision
  #   old:
  #     ansible_host: 198.51.100.10
  #     migration_source_ip: 198.51.100.10
  #     old_checkout_dir: /home/ubuntu/misskey.blue-docker-provision
  #     compose_project: misskeyblue-docker-provision

services:
  proxy:
    image: caddy:2
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - ../../shared/certificate.pem:/etc/ssl/certs/certificate.pem
      - ../../shared/key.pem:/etc/ssl/private/key.pem
      - caddy-data:/data
      - caddy-config:/config
      - caddy-logs:/var/log/caddy
    networks:
      - internal_network

  media-proxy:
    image: ghcr.io/chan-mai/media-proxy-rs:main@sha256:e7211ce759b6b98d3a5c06cfd865f8612039ee399fa68b1c9a00cef185259dc4
    restart: always
    environment:
      MEDIA_PROXY_CONFIG_PATH: /etc/media-proxy/config.json
    volumes:
      - ./config.json:/etc/media-proxy/config.json:ro
    networks:
      - internal_network

networks:
  internal_network:
    enable_ipv6: true
    ipam:
      config:
        - subnet: fd3c:9a51:7e09::/64

volumes:
  caddy-data:
  caddy-config:
  caddy-logs:
